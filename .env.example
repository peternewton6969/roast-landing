# Copy to .env and fill in from your Supabase project (Project Settings -> API).
# Both are safe to ship in a frontend when Row Level Security is configured
# (public read/insert on roasts, public insert on waitlist). See supabase/schema.sql.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
